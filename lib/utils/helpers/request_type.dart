/// Enumeration representing different types of HTTP requests.
enum RequestType {
  /// Represents a GET request.
  get,

  /// Represents a POST request.
  post,

  /// Represents a PUT request.
  put,

  /// Represents a PATCH request.
  patch,

  /// Represents a DELETE request.
  delete,
}
